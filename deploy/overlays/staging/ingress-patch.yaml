apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: deep-work-station
spec:
  tls:
  - hosts:
    - staging-deepwork.your-domain.com
    secretName: deep-work-station-staging-tls
  rules:
  - host: staging-deepwork.your-domain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: deep-work-station
            port:
              number: 80